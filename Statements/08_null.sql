-- This SQL statement retrieves all rows from the `users_table_exmp` where the `name` column is NULL. 

SELECT * users_table_exmp WHERE name IS NULL;

SELECT * FROM users_table_exmp WHERE name IS NOT NULL;

